return {
  "mini.ai",

  --opts = function()
  --  local ai = require("mini.ai").eu
  --  return {
  --    -- Add a custom definition for "inside next parameter"
  --    p = ai.gen_spec.treesitter({
  --      a = "@parameter.outer",   -- around parameter
  --      i = "@parameter.inner"    -- inside parameter
  --    }),
  --  }
  --end
}
